openapi: '3.0.3'

info:
  title: CSE183 Books Example
  version: 0.1.0
  description: CSE183 Node.js & Express Example

servers:
  - url: http://localhost:3010

paths:
  /v0/books:
    get:
      description: Returns all books
      responses:
        200:
          description: Books Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BooksResponse'
        default:
          description: Unexpected Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

    post:
      description: Creates a new book
      requestBody:
        description: Application to add
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Book'
      responses:
        201:
          description: Application response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Book'
        400:
          description: Invalid ISBN
        409:
          description: Conflict. Requested ISBN in use.
        default:
          description: unexpected error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'

  /v0/books/{isbn}:
    get:
      description: Returns a single book
      parameters:
      - name: isbn
        in: path
        description: ISBN of the book
        required: true
        schema:
          type: string
      responses:
        200:
          description: Book found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Book'
        400:
          description: Invalid ISBN
        404:
          description: Book not found
        default:
          description: Unexpected Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
                
components:
  schemas:
    Error:
      required:
        - code
        - message
      properties:
        code:
          type: integer
          format: int32
        message:
          type: string

    BooksResponse:
      type: object
      properties:
        books: 
          type: array
          items:
            $ref: '#/components/schemas/Book'

    Book:
      type: object
      properties:
        isbn:
          type: string
        title:
          type: string
        author:
          type: string
        publisher:
          type: string
      required:
        - title
        - author 
        - isbn 
        - publisher

